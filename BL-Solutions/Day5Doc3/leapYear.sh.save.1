#!/bin/bash -x
echo "Enter a year to check if it is leap or not: "
read year;
leap=false;
if [ $year%4==0 ];then
	if [ $yea % 100 == 0 ];then
		if [ $year % 400 == 0 ];then
			leap="true";
		else
			leap="false";
		fi
	else
		leap="true";
	fi
else
leap="false";
fi
echo $leap;
